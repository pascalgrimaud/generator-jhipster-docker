apply plugin: 'docker'

task buildDocker(type: Docker, dependsOn: build) {
  push = false
  applicationName = '<%=baseName.toLowerCase()%>'
  tagVersion = 'tmp'
  dockerfile = file('docker/push/Dockerfile')
  doFirst {
    copy {
      from "docker/push/"
      into stageDir
      include "*.*"
    }
    copy {
      from "build/libs"
      into stageDir
      include "*.war*"
    }
  }
}
